# Production Deployment Environment Variables
# The URL of the production backend API (required)
VITE_API_BASE_URL=https://api.yourdomain.com

# Controls which routes are bundled in the frontend.
# Set this to "public" for the Vercel deployment to exclude internal reception/director portals.
VITE_DEPLOY_TARGET=public
